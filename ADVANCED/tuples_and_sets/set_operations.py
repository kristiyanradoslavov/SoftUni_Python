
#1 set_union = 2 ways of using it
# union() or first_set | second set - !!!! basicaly removes duplicates !!!!

# 2 set_intersection =
# intersection() or first_set & second set - !!!! returns matching elements (intersecting) !!!!

# set_difference =
# difference() or first_set - second_set / second set - first set - !!!! returns the elements which are diff !!!!

# symetric difference =
# symetric_differecene() or first_set ^ second_set -
# !!!! returns only the elements which are present in the first and the second but not in both !!!!

# subset = a < b if a is a subset of b - returns True, else returns False
# superset = a>b if a is superset of b - returns True, else returns False

# a = {1, 2, 3, 4, 5}
# b = {1, 2, 3}
# print(a > b) ->>> returns True

# a.isdisjoint(b) - returns True if there are no common elements between both sets.